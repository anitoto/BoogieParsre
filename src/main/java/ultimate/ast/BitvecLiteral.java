/* BitvecLiteral -- Automatically generated by TreeBuilder (2025-06-30T08:11Z) */

package ultimate.ast;

import java.util.List;

import ultimate.ast.BoogieASTNode;
import ultimate.models.IBoogieType;
import ultimate.models.ILocation;
/**
 * Represents a bitvec literal which is a special form of a expression.
 */
public class BitvecLiteral extends Expression {
    private static final long serialVersionUID = 1L;
    private static final java.util.function.Predicate<BoogieASTNode> VALIDATOR = 
			BoogieASTNode.VALIDATORS.get(BitvecLiteral.class);
    /**
     * The value given as String. This representation is used to support
     * arbitrarily large numbers. We do not need to compute with them but
     * give them 1-1 to the decision procedure.
     */
    String value;

    /**
     * The number of bits in this bitvector.
     */
    int length;

    /**
     * The constructor taking initial values.
     * @param loc the location of this node
     * @param value the value given as String.
     * @param length the number of bits in this bitvector.
     */
    public BitvecLiteral(ILocation loc, String value, int length) {
        super(loc);
        this.value = value;
        this.length = length;
        assert VALIDATOR == null || VALIDATOR.test(this) : "Invalid BitvecLiteral: " + this;
    }

    /**
     * The constructor taking initial values.
     * @param loc the location of this node
     * @param type the type of this expression.
     * @param value the value given as String.
     * @param length the number of bits in this bitvector.
     */
    public BitvecLiteral(ILocation loc, IBoogieType type, String value, int length) {
        super(loc, type);
        this.value = value;
        this.length = length;
        assert VALIDATOR == null || VALIDATOR.test(this) : "Invalid BitvecLiteral: " + this;
    }

    /**
     * Returns a textual description of this object.
     */
    public String toString() {
        StringBuffer sb = new StringBuffer();
        sb.append("BitvecLiteral").append('[');
        sb.append(value);
        sb.append(',').append(length);
        return sb.append(']').toString();
    }

    /**
     * Gets the value given as String. This representation is used to support
     * arbitrarily large numbers. We do not need to compute with them but
     * give them 1-1 to the decision procedure.
     * @return the value given as String.
     */
    public String getValue() {
        return value;
    }

    /**
     * Gets the number of bits in this bitvector.
     * @return the number of bits in this bitvector.
     */
    public int getLength() {
        return length;
    }

    public List<BoogieASTNode> getOutgoingNodes() {
        List<BoogieASTNode> children = super.getOutgoingNodes();
        return children;
    }

    public void accept(GeneratedBoogieAstVisitor visitor) {
        if (visitor.visit((Expression)this)) {
            //visit parent types higher up if necessary
        } else {
            return;
        }
        visitor.visit(this);
    }

    public Expression accept(GeneratedBoogieAstTransformer visitor) {
        Expression node = visitor.transform(this);
        if(node != this){
            return node;
        }

        return this;
    }
}
